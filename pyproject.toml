[project]
name = "qqq"
version = "0.1.0"
description = "Web Interface for Sun Grid Engine."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.135.1",
    "uvicorn[standard]>=0.41.0",
    "pydantic>=2.12.5",
]

[dependency-groups]
dev = [
    "ruff>=0.15.5",
]
